interface:
  display_name: "Xprobe Latency Measurement"
  short_description: "Measure bounded CPU and GPU event latency"
  default_prompt: "Use $xprobe-measure-latency to measure bounded latency between two observable events."
