interface:
  display_name: "Xprobe Latency Measurement"
  short_description: "Measure host-to-GPU latency with evidence"
  default_prompt: "Use $xprobe-measure-latency to measure a bounded host-to-GPU latency path."
